{
  "name": "zomlab",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "with-env": "pnpm exec dotenv",
    "prepare": "pnpm exec husky",
    "reset": "pnpm run clean && pnpm install",
    "setup": "tsx scripts/setup.ts",

    "dev": "pnpm run kill:ports && pnpm run with-env -- pnpm exec turbo run web#dev",
    "dev:api": "pnpm run kill:ports && pnpm run with-env -- pnpm exec turbo run api#dev",
    "dev:standalone": "pnpm run kill:ports && pnpm run with-env -- pnpm exec turbo run dev",
    "dev:db": "docker compose up -d postgres redis",
    "dev:types": "pnpm exec turbo watch check-types",
    "dev:clean": "pnpm exec turbo clean && rm -rf .turbo",
    "dev:debug": "NODE_OPTIONS='--inspect=9229' pnpm run dev",
    "kill:ports": "kill $(lsof -tiTCP:3000-3005 -sTCP:LISTEN 2>/dev/null) 2>/dev/null || true",

    "build": "pnpm run with-env -- pnpm exec turbo run build",
    "build:api": "pnpm run with-env -- pnpm exec turbo run api#build",
    "build:web": "pnpm run with-env -- pnpm exec turbo run web#build",

    "check:all": "pnpm run with-env -- pnpm exec biome check . && pnpm exec syncpack lint && pnpm run deps:unused && pnpm exec turbo run check-types && pnpm exec vitest run",
    "check-types": "pnpm exec turbo run check-types",
    "check-types:watch": "pnpm exec turbo run check-types:watch",
    "lint": "pnpm exec biome check .",
    "lint:fix": "pnpm exec biome check --write .",
    "format": "pnpm exec biome format --write .",
    "format:check": "pnpm exec biome format --check .",

    "test": "pnpm exec vitest run",
    "test:watch": "pnpm exec vitest",
    "test:e2e": "kill $(lsof -tiTCP:${E2E_PORT:-3100} -sTCP:LISTEN 2>/dev/null) 2>/dev/null; pnpm exec playwright test",

    "storybook": "pnpm exec turbo run storybook",
    "build-storybook": "pnpm exec turbo run build-storybook",

    "db:generate": "pnpm run with-env -- pnpm exec turbo run db:generate --filter=@zomlab/database",
    "db:migrate": "pnpm run with-env -- pnpm exec turbo run db:migrate --filter=@zomlab/database",
    "db:push": "pnpm run with-env -- pnpm exec turbo run db:push --filter=@zomlab/database",
    "db:deploy": "pnpm run with-env -- pnpm exec turbo run db:deploy --filter=@zomlab/database",
    "db:studio": "pnpm run with-env -- pnpm exec turbo run db:studio --filter=@zomlab/database",

    "deps:check": "pnpm exec syncpack lint",
    "deps:fix": "pnpm exec syncpack fix",
    "deps:unused": "pnpm run with-env -- pnpm exec knip",

    "security:audit": "pnpm audit --prod",
    "security:check": "pnpm audit",

    "version:packages": "pnpm exec changeset version",
    "version:sync": "tsx scripts/sync-version.ts",
    "changeset": "pnpm exec changeset",

    "generate:package": "tsx scripts/generate-package.ts",

    "clean": "pnpm exec rimraf node_modules .turbo dist test-results generated apps/*/{node_modules,.turbo,dist,generated} packages/*/{node_modules,.turbo,dist,generated}"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@changesets/cli": "^2.31.1",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@playwright/test": "^1.62.0",
    "dotenv-cli": "^11.0.0",
    "husky": "^9.1.7",
    "jsdom": "^30.0.1",
    "knip": "^6.29.0",
    "lint-staged": "^17.2.0",
    "rimraf": "^6.1.3",
    "syncpack": "^15.3.2",
    "tsdown": "^0.22.14",
    "@types/node": "^26.1.2",
    "tsx": "4.23.5",
    "turbo": "2.10.7",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "lint-staged": {
    "*": "biome check --no-errors-on-unmatched --write --files-ignore-unknown=true",
    "*.{ts,tsx}": "biome check --no-errors-on-unmatched --write --files-ignore-unknown=true"
  },
  "engines": {
    "node": ">=24.18.1"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.20.0",
      "onFail": "error"
    }
  },
  "packageManager": "pnpm@11.20.0"
}
